




1. make sort plate labels (re-runable)


	if sql .db exits, ask for list of new plates to be added


	do you need any custom labels?

		if yes: provide list of custum plate names

	read csv with info for standard sample plates


	create random 5 char plate base name, then store full plate name in a dict with random 5 charactter echo/hamilton plate barcodes

	custom plates are own row in df

	make bartender file for hamilton and echo labels for each plate

	create sql .db.


2. Script to generate FA input files and take in layout files (re-runable)

read .db into dfs, key factor is identifying standard vs custom plates

how does user indicates which plates need analysis?  Maybe provide a list if plate names generated by scanning sorted plate barcodes. should be able to use name or barcode in the list and script figure it out.

Based on the expected plates the script looks up which ones are standard and which ones are custom. Standard plates will then refer to a template file that is stored within the Python script workflow folder. Any custom plates must have an individual custom layout file, and the script will search through the folder to find these files verify the correct format and then use them. Missing custom layout files will be flagged in the script will terminate.


Generate FA input files for a subset of columns. Ignore empty columns, ensure different groups are included. If cannot resolve, then ignore all empty columns and select the first 46 and last 46 wells column-wise if >96 wells are NOT unused.

update db table individualt plates identifying which plates had FA input files generated

move custom layout files to special folder

	
		
3. process FA output and separate endpoint DNA quant files for QC			




